package com.YouTube.Backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Indicates that this is a test class for a Spring Boot application
@SpringBootTest
class BackendApplicationTests {

    // Test method to check if the Spring application context loads correctly
    // This test ensures that the application can start without any configuration issues.
    @Test
    void contextLoads() {
        // No implementation needed here.
        // If the application context fails to load, this test will fail.
    }
}
